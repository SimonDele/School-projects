/*
#include "../tests/testsComportement.h"
#include<vector>
#include "../include/Capteur.h"
#include "../include/VecteurR3.h"


void testsComportement::testAllerPoint(void)
{
/*
    posInit = new VecteurR3(1,1,1);
    posFin = new VecteurR3(4,4,4);

    vector <Capteur> vCapteurs;
    for(int i=0; i<vCapteurs.size();i++)
    {
        vCapteurs[i].distanceDetectee=vCapteurs[i].portee;
    }

    VecteurR3 vecRes = Comportement.AllerPoint(posInit, posFin, Capteurs);
    VecteurR3 vecVerif = vecRes.prodVec((posFin-posInit));

    CPPUNIT_ASSERT(vecVerif.getX()==0 && vecVerif.getY()==0 && vecVerif.getZ()==0);
    */
//}


/*
void testsComportement::setUp(void)
{
    //Methode appelÈ dËs le debut, c'est ici qu'on est censÈ crÈer les objets
}

void testsComportement::tearDown(void)
{
    //C'est ici qu'on dÈtruit les objets
}
*/

